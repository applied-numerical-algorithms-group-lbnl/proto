add_subdirectory(_common)
add_subdirectory(LevelPoisson)
add_subdirectory(LevelEuler)
if(AMR)
    add_subdirectory(AMRPoisson)
    add_subdirectory(AMRAdvection)
    add_subdirectory(AMREuler)
endif()
if(MMB)
endif()
if (AMR AND MMB)
  add_subdirectory(MBPoisson)
endif()
